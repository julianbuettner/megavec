[package]
name = "megavec"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = { version = "2.0.1", features = ["serde"] }
lz4_flex = "0.12.0"
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
rand_chacha = "0.9.0"

[profile.bench]
debug = true
